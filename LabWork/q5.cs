using System;

class SimpleCalculator
{
    /*
    static void Main()
    {
        // Take all input from the user at once
        Console.Write("Enter the first number: ");
        double num1 = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter the second number: ");
        double num2 = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter an operator (+, -, *, /): ");
        char operation = Console.ReadLine()[0]; // Taking the first character as operator

        // Perform all operations after input is taken
        double result = 0;
        bool isValid = true;

        if (operation == '+')
        {
            result = num1 + num2;
        }
        else if (operation == '-')
        {
            result = num1 - num2;
        }
        else if (operation == '*')
        {
            result = num1 * num2;
        }
        else if (operation == '/')
        {
            if (num2 != 0)
            {
                result = num1 / num2;
            }
            else
            {
                Console.WriteLine("Error: Division by zero!");
                isValid = false;
            }
        }
        else
        {
            Console.WriteLine("Error: Invalid operator!");
            isValid = false;
        }

        // Print the result if the operation is valid
        if (isValid)
        {
            Console.WriteLine("Result: " + result);
            Console.ReadKey();
        } */
    }

